(function () {
	'use strict';

	/**
	 * ES6 brings many enhancements to the already built-in javascript objects.
	 * This series of tests will show you these additions and how to use them.
	 */
	xdescribe('ES2015 Built-in Objects Enhancements', function () {

		/**
		 * The `Math` object already had many mathematical functions in ES5, and introduces many
		 * more with the ES6 (ES2015) release.
		 *
		 * @see [MDN Reference: Math](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Math)
		 * @see [ECMA 262 The Math Object](https://tc39.github.io/ecma262/#sec-math-object)
		 */
		xdescribe('The Math object', function () {
			/*
			 * I decided not to go over these because there is a ton of new Math features and there isn't
			 * much benefit to creating tests over reading the documentation for these features.
			 */

			xit('', function () {
				expect().toBe();
			});
		});
	});
})();
